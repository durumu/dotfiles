- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc:
    ~/.zshrc:
    ~/.xinitrc:
    ~/.ghci:
    ~/.vim:
    ~/.aliases:
    ~/.config/:
      glob: true
      path: config/*
    ~/.oh-my-zsh/themes/agnoster2.zsh-theme:
      path: agnoster2.zsh-theme
    ~/.latex:
