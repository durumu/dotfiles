- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/:
      glob: true
      path: config/*
    ~/.profile:        
    ~/.zshrc:
    ~/.aliases:
    ~/.vimrc:
      path: config/nvim/init.vim
    ~/.gitconfig:
    ~/.gitignore:
    ~/.ghci:
    ~/.latex:
