- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/:
      glob: true
      path: config/*
    ~/.zshrc:
    ~/.aliases:
    ~/.gitconfig:
    ~/.gitignore:
    ~/.ghci:
    ~/.latex:
    ~/.oh-my-zsh/custom/themes/durumu.zsh-theme:
      path: durumu.zsh-theme
