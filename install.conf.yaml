- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.profile:        
    ~/.vimrc:
    ~/.zshrc:
    ~/.aliases:
    ~/.ghci:
    ~/.vim:
    ~/.config/:
      glob: true
      path: config/*
    ~/.oh-my-zsh/themes/agnoster2.zsh-theme:
      path: agnoster2.zsh-theme
    ~/.latex:
