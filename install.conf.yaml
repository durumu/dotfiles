- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim:
    ~/.zshrc:
    ~/.aliases:
    ~/.gitconfig:
    ~/.gitignore:
    # ~/.oh-my-zsh/custom/themes/durumu.zsh-theme:
    #   path: durumu.zsh-theme
